# Prompt Tool

Prompt Tool 领域将提示词注入行为组织为可选预设。本词汇表区分配置结构、运行时阶段和持久上下文。

## Language

### 预设与注入

**预设（Preset）**：
用户可选择的完整注入方案，组合提示词配置、能力模块、模型路由和模板变量。
_Avoid_: profile、配置包、角色卡

**提示词配置（Prompt Config）**：
一条独立命名、可单独启停的注入规则，定义内容或控制行为及其插入点、策略和适用条件。
_Avoid_: 提示词、能力模块、配置卡

**能力模块（Capability Module）**：
预设显式选用的可复用行为单元，用于增加一类运行时能力。
_Avoid_: 插件、提示词配置

**组合（Composition）**：
一个预设中能力模块及其关系构成的可执行行为结构。
_Avoid_: 预设、模块列表

**插入点（Injection Point）**：
提示词配置参与会话或请求生命周期的具名位置，各插入点具有独立语义。
_Avoid_: 位置、全局层级

**位置（Position）**：
一条提示词配置在所属插入点内相对于锚点的落位。
_Avoid_: 插入点、顺序

**层内顺序（Order）**：
同一插入点内多条提示词配置的比较次序。
_Avoid_: 全局顺序、插入点顺序

**内容策略（Strategy）**：
提示词配置选择、填充或触发内容的规则，与插入点正交。
_Avoid_: 插入点、时机

**时机（Timing）**：
提示词配置在会话生命周期中满足何种阶段或次数条件时生效。
_Avoid_: 位置、内容策略、消息受众

**消息受众（Audience）**：
提示词配置的注入对象：主会话、子代理或两者。
_Avoid_: 模型范围、会话范围

### 配置值

**预设参数（Preset Parameter）**：
归属单个预设、用于调节能力模块行为的值。
_Avoid_: 部署设置、模板变量

**部署设置（Deployment Setting）**：
对整个插件安装生效、不属于任一预设行为的开关或路径。
_Avoid_: 预设参数、模块配置

**模板变量（Template Variable）**：
归属预设或提示词配置的命名内容默认值，在注入内容中用于插值。
_Avoid_: 预设参数、会话变量

**会话变量（Session Variable）**：
仅在当前会话内持续的可变内容值，可覆盖同名内容默认值。
_Avoid_: 模板变量、角色记忆

### 运行时生命周期

**Epoch**：
从会话开始或上一次成功压缩边界开始、到下一次成功压缩边界结束的运行窗口，晋升与门控状态在其中计算。失败压缩不创建新 Epoch；模型可见内容的去重由其 surface 生命周期决定。
_Avoid_: 会话、轮次

**晋升（Promotion）**：
同一 epoch 内从受限的初始阶段进入完整能力阶段的状态转换。
_Avoid_: 模型升级、权限扩张、会话初始化

**PTC 呈现（Code Mode）**：
让模型通过单个程序编排多步工具操作的工具呈现方式。
_Avoid_: 晋升、工具窄化、PTC 门控

### 角色与上下文

**人设（Persona）**：
当前预设为主会话或子代理提供的系统身份。
_Avoid_: 角色卡、SillyTavern 预设卡片

**SillyTavern 预设卡片（SillyTavern Preset）**：
包含提示词列表、可转换为 Prompt Tool 预设的外部配置资产。
_Avoid_: 角色卡、本地预设

**角色卡（Character Card）**：
可移植的角色定义，可包含人设、开场白和世界书，并可应用到一个预设。
_Avoid_: SillyTavern 预设卡片、人设、本地预设

**世界书条目（World Book Entry）**：
一个可常驻或由关键词触发的上下文知识单元。
_Avoid_: 角色记忆、提示词配置集

**角色记忆（Character Memory）**：
归属角色卡、跨会话和跨预设复用的长期上下文。
_Avoid_: 会话变量、世界书条目

### 子代理授权

**子代理工具策略（Subagent Tool Policy）**：
在授权上限内为每个子代理选择工具档与受控扩权的规则集。
_Avoid_: 主会话工具过滤、能力模块
