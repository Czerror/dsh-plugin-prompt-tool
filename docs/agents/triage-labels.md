# Triage Labels

| 技能中的标签      | 本仓库中的标签      | 含义                     |
| ----------------- | ------------------- | ------------------------ |
| `needs-triage`    | `needs-triage`      | 等待维护者评估           |
| `needs-info`      | `needs-info`        | 等待报告者补充信息       |
| `ready-for-agent` | `ready-for-agent`   | 规格完整，可交由代理实现 |
| `ready-for-human` | `ready-for-human`   | 需要人工实现               |
| `wontfix`         | `wontfix`           | 不会处理                   |

技能提及某个 triage 角色时，使用表中对应的本仓库标签。
