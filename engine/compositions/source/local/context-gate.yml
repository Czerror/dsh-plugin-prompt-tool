# module: context-gate
# source: engine/compositions/source/local/context-gate.yml (本地附加,上游 dsh-anchored-standard 移植)
# 首轮统一关闭 runtime-context 与未放行的 pre-step 注入；持久 tool/call 或
# assistant/message 晋升后恢复，compaction/end 开启新 epoch。

- id: context-gate
  name: ./engine/context-gate.mjs
  config:
    promoteOn: either
    includeSubagents: false
