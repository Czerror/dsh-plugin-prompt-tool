# 模板：主会话人设（system-section 段，同名 shadow 全局 persona）
# params.sectionName = deployment:persona 时注册为主会话人设段：
#   - complete: true 独占整个 system prompt（预设内互斥，UI 自动收敛）
#   - suppressRuntimeContext: true 抑制动态运行时快照
# 子代理经 scope 链继承本段；如需子代理独立人设，用「子代理模型」卡 subagentPersona。
id: persona-main
name: 主会话人设
layer: system-section
strategy: static
order: 0
text: |-
  You are a helpful assistant.

  Think deeply first, then produce.
variables: {}
params:
  sectionName: deployment:persona
  complete: true
  suppressRuntimeContext: true
