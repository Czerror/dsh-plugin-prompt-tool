# 模板：tool-pipeline 工具调用三阶段（pre-execute / execute / post-execute）
id: example-tool-pipeline
layer: tool-pipeline
strategy: static
order: 0
modelScope: all
text: ""            # postAction=replace/block 时使用的文本
params:
  toolNames: bash,run_code
  preDecision: allow
  denyReason: 该工具被提示词配置禁用
  postAction: accept
