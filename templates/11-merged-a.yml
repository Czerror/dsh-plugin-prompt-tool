# 模板：merged 拼接（同 position 的 merged 配置拼接为一条消息）
# 本模板演示完整参数（mergeMode + texts 多块）；复制本文件为多条同 position
# 配置并调整 order，即演示多配置拼接（UI 卡片可复制/新建）。
id: example-merged-a
layer: pre-step
strategy: static
position: before-all
mergeMode: merged
order: 0
texts:
  - 合并段 A1
  - 合并段 A2
