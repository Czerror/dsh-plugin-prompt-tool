# 自定义工具模板：委托内置 character_list（自定义名称/描述/参数面；
# 内置工具为插件固有注册，原工具仍可用）。
id: delegate-character-list
name: my_character_list
description: 查看角色卡库（委托内置工具）
output:
  schema:
    type: object
    additionalProperties: true
    properties:
      ok: { type: boolean }
      value: { type: json }
execute:
  kind: delegate
  tool: character_list
