# 自定义工具模板：委托内置 character_list（隐藏原工具 + 自定义名称/描述）。
# 配合 preset.yml builtinTools.character.enabled=false 使用，避免双份暴露。
id: delegate-character-list
name: my_character_list
description: 查看角色卡库（委托内置工具）
output:
  schema:
    type: object
    additionalProperties: true
    properties:
      ok: { type: boolean }
      value: { type: json }
execute:
  kind: delegate
  tool: character_list
