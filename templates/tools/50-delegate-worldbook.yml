# 自定义工具模板：委托内置 world_book_list（自定义名称/描述/参数面；
# 保存该模板时自动装配 world-book-tools，原工具仍可用）。
id: delegate-worldbook-list
name: my_worldbook_list
description: 查看当前预设的世界书条目（委托内置工具）
output:
  schema:
    type: object
    additionalProperties: true
    properties:
      ok: { type: boolean }
      value: { type: json }
execute:
  kind: delegate
  tool: world_book_list
