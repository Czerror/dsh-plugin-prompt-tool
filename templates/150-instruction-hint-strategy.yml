# 模板：指令文件提示（strategy=instruction-hint 快捷写法）
# 独立策略直接读取 agents-instruction.txt 注入（等价 placeholder + fill=instruction-hint）。
# 提示：把本文件放入 promptConfigsDir 目录，或把对象写入 settings.promptConfigs 数组。
id: example-instruction-hint-strategy
name: 示例：指令文件提示（策略快捷写法）
enabled: true
layer: pre-step
strategy: instruction-hint
configKind: ordered
order: 31
role: user
priority: 0
mergeMode: separate
sourceKind: instruction-hint
form: hint
identity:
  field: kind
  value: instruction-hint
