# 模板：llm-stream 模型流包装（pass 透传 / replace 用 text 替代整个流）
id: example-llm-stream
layer: llm-stream
strategy: static
modelScope: flash
order: 0
text: ""            # mode=replace 时替代整个模型流的文本
params:
  mode: pass
